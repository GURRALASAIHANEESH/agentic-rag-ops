# ─────────────────────────────────────────────────────────────
# Agentic RAG Ops — Backend Dependencies
# Pinned to exact versions for reproducible builds.
# Update with: pip-compile requirements.in
# ─────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # required for UploadFile support

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0                # async Postgres driver
alembic==1.13.1                # database migrations
psycopg2-binary==2.9.9         # sync driver (used by Alembic only)
pgvector==0.2.5                # pgvector SQLAlchemy type

# ── Auth & Security ───────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # password hashing
bcrypt==4.1.3

# ── Settings ──────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.1

# ── Embeddings ────────────────────────────────────────────────
sentence-transformers==2.7.0   # all-MiniLM-L6-v2
torch==2.3.0                   # CPU-only build (see note below)
numpy==1.26.4
transformers==4.41.1
huggingface-hub==0.23.2
tokenizers==0.19.1

# ── Vector store ──────────────────────────────────────────────
faiss-cpu==1.8.0               # FAISS fallback (CPU build, no GPU needed)

# ── PDF processing ────────────────────────────────────────────
pypdf==4.2.0                   # pure-Python PDF parser, no system deps

# ── LLM clients ───────────────────────────────────────────────
httpx==0.27.0                  # async HTTP for llama.cpp server
openai==1.30.1                 # OpenAI SDK (also used by Groq)
groq==0.9.0                    # Groq SDK (free tier)

# ── Rate limiting ─────────────────────────────────────────────
slowapi==0.1.9                 # FastAPI rate limiter (wraps limits)

# ── Observability ─────────────────────────────────────────────
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==6.1.0
structlog==24.1.0              # structured JSON logging

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                  # AsyncClient for FastAPI testing
respx==0.21.1                  # mock httpx requests in tests
factory-boy==3.3.0             # test data factories
aiosqlite==0.20.0              # async SQLite driver for in-memory test DB

# ── Dev tools (not installed in production image) ─────────────
# black==24.4.2
# ruff==0.4.4
# mypy==1.10.0
